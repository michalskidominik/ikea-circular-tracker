/**
 * Reprezentuje informacje o sortowaniu.
 */
export interface Sort {
  /** Czy jest niesortowane. */
  unsorted: boolean;

  /** Czy jest posortowane. */
  sorted: boolean;

  /** Czy jest puste. */
  empty: boolean;
}
